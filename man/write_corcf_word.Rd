% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corcf_docx_output.R
\name{write_corcf_word}
\alias{write_corcf_word}
\title{Write corcf results to a Word document}
\usage{
write_corcf_word(
  res,
  master_path,
  using_path,
  id_cols = NULL,
  path = "corcf_results.docx",
  report_date = Sys.Date()
)
}
\arguments{
\item{res}{A result object returned by \code{corcf()}.}

\item{master_path}{Character scalar. Path to the master data source.}

\item{using_path}{Character scalar. Path to the using data source.}

\item{id_cols}{Optional character vector of ID columns used for comparison.}

\item{path}{Character scalar. Output path for the \code{.docx} report.}

\item{report_date}{Date for the report header. Defaults to \code{Sys.Date()}.}
}
\value{
Invisibly returns the output file path.
}
\description{
Create a Word report from a \code{corcf()} result object, including summary
counts, per-variable comparison results, and value-label conflicts.
}
\examples{
res <- list(
  ecode = 9L,
  n_master = 3L,
  n_using = 3L,
  type_mismatch = 0L,
  value_mismatch = 1L,
  merge_counts = list(
    not_matched = 0L,
    master_only = 0L,
    using_only = 0L,
    matched = 3L
  ),
  per_variable = list(
    x = list(status = "mismatch", n = 1L),
    y = list(status = "match", n = 0L)
  ),
  label_conflicts = list(),
  vars_missing_in_using = character(),
  vars_missing_in_master = character()
)

tmp <- tempfile(fileext = ".docx")
\dontrun{
  write_corcf_word(
    res = res,
    master_path = "master.csv",
    using_path = "using.csv",
    id_cols = c("id"),
    path = tmp
  )
}
}
