% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corcf_function.R
\name{corcf}
\alias{corcf}
\title{Compare two datasets by key and variable values}
\usage{
corcf(
  master,
  using,
  vars = NULL,
  id = NULL,
  verbose = FALSE,
  verbose1 = NULL,
  all = FALSE,
  masterlist = FALSE,
  usinglist = FALSE,
  separator = NULL,
  sepby = NULL,
  reldif = NULL,
  nodecrease = FALSE,
  label_conflicts = TRUE,
  print_label_conflicts = TRUE
)
}
\arguments{
\item{master}{A data frame containing the master data.}

\item{using}{A data frame containing the using data, or a path to a CSV file.}

\item{vars}{Character vector of variables to compare. Use \code{"_all"} or \code{NULL}
to compare all variables except the ID columns.}

\item{id}{Optional character vector of unique ID columns.}

\item{verbose}{Logical; if \code{TRUE}, print mismatched records.}

\item{verbose1}{Optional positive integer limiting the number of mismatched
rows printed per variable. Also turns on \code{verbose}.}

\item{all}{Logical; if \code{TRUE}, also print variables that match.}

\item{masterlist}{Logical; if \code{TRUE}, print IDs found only in \code{master}.}

\item{usinglist}{Logical; if \code{TRUE}, print IDs found only in \code{using}.}

\item{separator}{Optional positive integer controlling grouped printing of
mismatch output.}

\item{sepby}{Optional character vector of columns used to sort mismatch output.}

\item{reldif}{Optional numeric tolerance for relative difference comparisons
on numeric variables.}

\item{nodecrease}{Logical; only report relative-difference mismatches where
the master value is greater than or equal to the using value.}

\item{label_conflicts}{Logical; if \code{TRUE}, detect value-label conflicts.}

\item{print_label_conflicts}{Logical; if \code{TRUE}, print detected label conflicts.}
}
\value{
A list of class \code{"corcf_result"} containing merge counts, error code,
mismatch summaries, per-variable results, label conflicts, and variable
availability summaries.
}
\description{
Compare a master dataset and a using dataset across selected variables,
optionally keyed by one or more ID columns. The function reports merge-style
counts, type mismatches, value mismatches, and value-label conflicts.
}
\examples{
master_df <- data.frame(
  id = c(1, 2, 3),
  visitdate = c("2024-01-01", "2024-01-02", "2024-01-03"),
  x = c(10, 20, 30),
  stringsAsFactors = FALSE
)

using_df <- data.frame(
  id = c(1, 2, 3),
  visitdate = c("2024-01-01", "2024-01-02", "2024-01-03"),
  x = c(10, 21, 30),
  stringsAsFactors = FALSE
)

res <- corcf(
  master = master_df,
  using = using_df,
  vars = "x",
  id = c("id", "visitdate")
)

res$ecode
res$value_mismatch
}
